import React from 'react';
import FullWidthImage from './FullWidthImage';

const PhotoToContactBuffer: React.FC = () => {
    return (
        <FullWidthImage
            src="/assets/buffers/photo-contact-buffer.svg"
            alt="Hero to Project Buffer"
        />
    );
};

export default PhotoToContactBuffer;
