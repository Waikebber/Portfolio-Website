import React from 'react';
import FullWidthImage from './FullWidthImage';

const ResumeToPhotoBuffer: React.FC = () => {
    return (
        <FullWidthImage
            src="/assets/buffers/resume-photo-buffer.svg"
            alt="Hero to Project Buffer"
        />
    );
};

export default ResumeToPhotoBuffer;
